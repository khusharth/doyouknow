import React from "react";
import "./header.scss";
import Logo from "../Logo/Logo";

const Header = () => {
    return (
        <header className='header'>
            <Logo />
        </header>
    );
};

export default Header;
